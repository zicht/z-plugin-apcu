# `apcu` plugin for `zicht/z`

Provides a task to clear a remote 'apcu' cache, by uploading
a file, fetching it with wget and removing the file.

Also hooks into the deploy.post trigger.

# Maintainer(s)
* Philip Bergman <philip@zicht.nl>
